apiVersion: v1
kind: Pod
metadata:
  name: static-web-test-local
  labels:
    role: myrole
spec:
  containers:
    - name: web1
      image: nginx
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
